/*
import javax.swing.*;
import java.util.Random;

public class GissaTalet {
    public static void main(String []args) {
      Random rand = new Random();
        int rand_int2 = rand.nextInt(100);
            System.out.println("Random Integers: "+rand_int2);
        int x;
        x = Integer.parseInt(JOptionPane.showInputDialog("Gissa på ett tal mellan 1 till 100!"));
        if (x < rand_int2) {
            Integer.parseInt(JOptionPane.showMessageDialog(null,"Du gissade för lågt! Gissa igen."));
        }
        if (x > rand_int2) {
            Integer.parseInt(JOptionPane.showMessageDialog(null, "Du gissade för högt! Gissa igen."));
        }
        if (x == rand_int2) {
            Integer.parseInt(JOptionPane.showMessageDialog(null,"Du gissade rätt! Grattis!"));
        }
    }
}*/
